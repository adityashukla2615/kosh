# Which brain the advisor uses: auto | anthropic | offline
# auto = anthropic when ANTHROPIC_API_KEY is set, otherwise offline.
# Offline is a real mode: the same tools and the same numbers, plainer wording.
LLM_PROVIDER=auto

ANTHROPIC_API_KEY=
KOSH_MODEL=claude-opus-5

PORT=8787
